{
  "name": "checkflow-server",
  "version": "1.0.0",
  "description": "CheckFlow API Server (Phase 1 MVP)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "reset": "rm -f data/checkflow.db && echo 'DB reset. Run npm start to recreate.'"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "better-sqlite3": "^11.7.0"
  }
}
